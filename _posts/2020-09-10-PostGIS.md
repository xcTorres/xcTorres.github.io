---
layout:     post
title:      "PostGIS SQL使用"
date:       2020-09-10
author:     "xcTorres"
header-img: "img/in-post/osm/osm-bg.png"
catalog:    true
mathjax: true
tags:
    - GIS
---  


## TBD

```bash

    docker network create
    docker-compose build
    docker-compose up

```

```bash

    docker run -it --network postgis --link postgis_postgis_1 --rm postgres  sh -c 'exec psql -h postgis -p "$POSTGRES_PORT_5432_TCP_PORT" -U postgres'

```

```bash

    osm2pgsql -s -U postgres -H 127.0.0.1 -P 5432 -W -d postgres ~/Desktop/jakarta.osm.pbf

```

```bash

    docker run -it --rm postgres  --network postgis sh -c 'exec psql -h postgres -p "$POSTGRES_PORT_5432_TCP_PORT" -U postgres'

    CREATE EXTENSION IF NOT EXISTS postgis;
    CREATE EXTENSION IF NOT EXISTS postgis_topology;


```
